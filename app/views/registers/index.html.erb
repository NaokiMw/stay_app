<h4>登録済みルーム一覧</h4>

<% if flash[:notice] %>
  <p class="notice"><%= flash[:notice] %></p>
<% end %>

<table>
  <thead>
    <tr>
      <th>ルーム画像</th>
      <th>ルーム名</th>
      <th>ルーム紹介</th>
      <th>料金</th>
      <th>作成日</th>
    </tr>
  </thead>

  <tbody>
    <% @registers.each do |register| %>
      <tr>
        <td><%= image_tag @register.image.url, class: 'rounded-circle mr15', width: '40', height: '40' %></td>
        <td><%= register.name_room %></td>
        <td><%= register.int_room %></td>
        <td><%= register.price %></td>
        <td><%= register.created_at  %></td>
      </tr>
    <% end %>
  </tbody>

</table>
