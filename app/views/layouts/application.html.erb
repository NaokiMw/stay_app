<!DOCTYPE html>
<html>
  <head>
    <title>StayApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class = "header">
      <div class = "headerinner">

        <div class = "headerleft">
          <div class = "h_leftitem">
            <a href="/home/index">,poteshare</a>
          </div>
          <div class = "h_leftitem">
          <%= search_form_for @q, url: registers_search_path do |f| %>
            <%= f.search_field :adress_cont,placeholder:"エリア" %>
            <% f.submit %>
          <% end %>

          </div>
          <div class = "h_leftitem">
          <%= search_form_for @q, url: registers_search_path do |f| %>
            <%= f.search_field :int_room_cont,placeholder:"キーワード" %>
            <% f.submit %>
          <% end %>
          </div>

        </div>

        <div class = "headerright">
        <%if user_signed_in? %>
          ログインユーザー: <%= current_user.name %>

          <input id="menu-cb" type="checkbox" value="off">
          <span class="icon_image"> <img src=<%= current_user.image %> class = "icon_image"></span>
          <label id="menu-icon" for="menu-cb"></label>

          <div id="ham-menu">
          <ul class="header-items">
            <li class="header-item"><a href="/registers/new">ルーム登録</a></li>
            <li class="header-item"><a href="/reserves/show">予約済ルーム一覧</a></li>
            <li class="header-item"><a href="/registers/index">登録済ルーム一覧</a></li>
            <li class="header-item"><a href="/users/edit">設定</a></li>
            <li class="header-item"><a href="destroy_user_session_path">ログアウト</a></li>
          </ul>
          </div>

          <% else %>
            <%= link_to "ログイン", new_user_session_path %>
            <%= link_to "新規登録", new_user_registration_path %>
          <% end %>
        </div>
      </div>
    </header>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>


    <%= yield %>
  </body>
</html>
